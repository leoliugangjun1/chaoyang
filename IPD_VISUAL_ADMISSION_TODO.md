# IPD 产品视觉准入审核智能体开发清单

## 执行规则

- 本清单严格按编号顺序执行；每次只将一个任务更新为已完成。
- 每轮开始前明确修改目标、允许修改范围、禁止破坏的逻辑和验收标准。
- 每完成一个模块，验证主流程、加载中、空数据和接口错误状态。
- 验证通过后，只暂存该任务涉及文件并提交，提交信息格式为 `feat: <模块>通过`。
- 业务规则唯一来源为 `docs/产品事实校验/` 与已发布 Skill 快照；不得将规则硬编码为可覆盖的前端结论。

## 01. 隔离项目骨架与本地运行环境

- [x] 修改目标：创建独立的 React + TypeScript 前端、Python HTTP 服务和本地配置模板。
- 允许修改范围：`ipd-visual-admission-app/`、`docs/产品事实校验/`、本文件。
- 不允许破坏的逻辑：不得改动现有 `app/`；不得接入云服务、写入密钥或生成业务审核结论。
- 验收标准：本地页面与健康检查可访问；页面具备加载中、空状态和接口错误状态；前后端构建检查通过。

## 02. 本地持久化与 Excel Parser 适配器

- [ ] 修改目标：实现项目、文件版本、Skill 版本、任务与报告的 SQLite 持久化；接入 `excel_parser` 适配器。
- 允许修改范围：`ipd-visual-admission-app/backend/`、相关 API、测试与本文件。
- 不允许破坏的逻辑：原始 Excel、解析快照、规则原文和历史报告不可覆盖；不支持未配置模板的静默解析。
- 验收标准：创建项目、上传固定 Excel、解析快照和来源定位均可刷新追溯；空文件、模板错误和解析错误可见。

## 03. Skill 注册、版本绑定与任务输入页

- [ ] 修改目标：读取已发布 Skill 快照，实现任务创建、文件元数据、Skill 选择和启动前校验。
- 允许修改范围：项目与规则服务、输入页组件、测试与本文件。
- 不允许破坏的逻辑：已发布 Skill 不可自动修改；任务必须绑定文件和 Skill 版本；前端不得保存唯一事实来源。
- 验收标准：可创建项目、上传、选择版本并启动；无文件、加载和接口错误均有明确状态。

## 04. A-C 阶段：资料定位、一票退回与完整度

- [ ] 修改目标：实现 A-C 的异步任务、阶段持久化、三项一票退回和完整度计算。
- 允许修改范围：编排服务、报告服务、进度页、测试与本文件。
- 不允许破坏的逻辑：一票退回不能被完整度覆盖；信息不足必须进入人工复核；完整度只检查字段存在性。
- 验收标准：固定顺序、阶段进度、失败原因和评分可复核；常规、证据不足、跨资料冲突的结果符合规则。

## 05. D-F 阶段：卖点证据、展示方案与通信检查

- [ ] 修改目标：逐条执行 USP/VOC、功能验证、证据范围、展示方案、场景/Slogan/流量词检查。
- 允许修改范围：LLM 适配器、编排服务、任务页、测试与本文件。
- 不允许破坏的逻辑：每次调用只处理一个规则或 `claim_id`；VOC、竞品或视觉建议不能证明本产品功能；不得伪造来源。
- 验收标准：任务包最小化、JSON 重试一次、无效输出进入人工复核；加载、空结果和 API 错误可见。

## 06. G 阶段与报告导出

- [ ] 修改目标：汇总阶段 JSON，校验 `report-schema.json`，持久化并导出 JSON/Markdown。
- 允许修改范围：报告与导出服务、API、测试与本文件。
- 不允许破坏的逻辑：汇总器不得读取原始资料或推翻确认的一票退回；导出不得包含 Prompt、调试日志或密钥。
- 验收标准：报告可按文件与 Skill 版本复现，Schema 校验通过，导出文件与页面字段一致。

## 07. 审核任务、进度与报告界面

- [ ] 修改目标：完成三页审核流程、进度/取消/重试/人工复核和报告筛选、展开、来源抽屉。
- 允许修改范围：前端页面、组件、样式、测试与本文件。
- 不允许破坏的逻辑：前端不计算或编辑准入结论；取消任务不能作为正式报告展示；来源缺失不得补造路径。
- 验收标准：四种状态、一票退回、完整度、筛选、长文本和移动布局均符合 UI&UX 文档。

## 08. 端到端验收与可靠性

- [ ] 修改目标：覆盖三组评估案例及取消、重试、加载、空数据、接口失败主流程。
- 允许修改范围：测试、必要的修复和本文件。
- 不允许破坏的逻辑：不得用测试特例绕过业务规则；不得修改历史记录。
- 验收标准：所有评估案例产生预期状态，端到端验证记录完整，本地应用可启动。
