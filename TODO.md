# 服装视觉设计分析智能体开发清单

## 执行规则

- 严格按编号顺序实施，每轮只将一个任务标记为已完成。
- 每个任务完成后，验证主流程、加载中、空数据和接口错误状态。
- 验证通过后，仅暂存本次任务涉及的文件，并创建一次 Git 提交。
- 任何任务不得破坏 `docs/ARCHITECTURE.md` 第 16 节的核心契约。

## 01. 项目骨架与本地运行环境

- [ ] 修改目标：建立隔离的前后端项目骨架、本地配置模板和开发脚本。
- 允许修改范围：`app/`、`.gitignore`、`.env.example`、本文件。
- 不允许破坏的逻辑：不接入登录、云服务或前端内存持久化；不写入密钥。
- 验收标准：前端和后端均可本地启动；健康检查、加载、空页面和接口错误提示可验证。

## 02. 项目、版本与文件持久化

- [ ] 修改目标：实现 SQLite 项目和版本记录、本地文件保存、Markdown 上传、Excel 转 Markdown 与图片保存。
- 允许修改范围：`app/backend/`、`app/frontend/` 中项目输入所需文件、`app/projects/`、本文件。
- 不允许破坏的逻辑：新资料必须创建新版本；原始文件和转换文件不得覆盖；前端状态不能作为唯一数据源。
- 验收标准：创建项目后刷新仍可查看；上传 Markdown、Excel、图片后均可追溯；空文件和保存失败有明确提示。

## 03. 三页路由与项目控制台

- [ ] 修改目标：实现三阶段导航、项目选择、版本切换和项目输入页面。
- 允许修改范围：`app/frontend/src/pages/`、`app/frontend/src/components/`、路由与项目 API、本文件。
- 不允许破坏的逻辑：阶段不可自动跳转；第二、三阶段在前置条件未满足时不可启动。
- 验收标准：路由刷新可恢复项目上下文；无项目、加载中和接口失败状态明确；窄屏不出现内容重叠。

## 04. 规则注册与版本绑定

- [ ] 修改目标：实现三类规则保存、选择、停用与版本绑定。
- 允许修改范围：规则服务、规则 API、规则选择组件、`app/rules/`、本文件。
- 不允许破坏的逻辑：每种类型每个项目只绑定一条规则；规则原文不自动修改；历史规则只停用不删除。
- 验收标准：可上传并选择规则；新版本保留绑定快照；空规则、重复类型和接口失败可处理。

## 05. 第一阶段：产品理解与人工确认

- [ ] 修改目标：实现产品事实、卖点证据的动态解析、编辑、确认与确认闸门。
- 允许修改范围：产品理解 Agent、事实与证据服务/API、第二页相关组件、测试、本文件。
- 不允许破坏的逻辑：字段不得在主流程硬编码；Markdown 卖点顺序最高且不可被补充资料重排；必须人工确认。
- 验收标准：任务显示加载、空资料和失败状态；确认前不可开始市场分析；确认后数据持久化。

## 06. 第二阶段：调研、权重与图片确认

- [ ] 修改目标：实现可替换的调研插口、权重应用、图片候选弹窗和图片审核。
- 允许修改范围：市场分析与证据服务/API、图片存储、第二页调研组件、测试、本文件。
- 不允许破坏的逻辑：权重默认为 Markdown 8、联网 2，总和固定为 10，且不能改变 Markdown 卖点顺序；未采用图片不得进入第三阶段。
- 验收标准：权重保存并重新计算展示；空候选、数据不足、加载和来源失败状态明确；弹窗关闭不丢失选择。

## 07. 第三阶段：结构化视觉看板与编辑

- [ ] 修改目标：实现由规则定义结构的视觉看板、采用图片过滤和允许字段编辑。
- 允许修改范围：视觉策略 Agent、看板服务/API、第三页组件、测试、本文件。
- 不允许破坏的逻辑：不固定模块结构或图片数量；未确认图片不可传入；编辑不能改变规则定义的整体结构或来源记录。
- 验收标准：看板可在刷新后恢复；未知模块给出结构化错误；空内容、加载和接口错误状态明确。

## 08. 导出 PDF 与图片 ZIP

- [ ] 修改目标：生成第三页正式内容 PDF，以及当前看板采用图片的 ZIP。
- 允许修改范围：导出服务/API、第三页导出控件、测试、本文件。
- 不允许破坏的逻辑：PDF 不含来源、权重、任务状态、版本或调试信息；ZIP 仅包含当前看板图片。
- 验收标准：可下载有效 PDF 和 ZIP；无图片、导出中和导出失败状态明确；编辑结果不会丢失。

## 09. 任务可靠性、取消与版本历史

- [ ] 修改目标：补齐任务进度持久化、自动重试、取消清理和历史版本浏览。
- 允许修改范围：任务和版本服务/API、状态组件、测试、本文件。
- 不允许破坏的逻辑：取消后本次中间结果和进度不保留；旧版本、原始文件、来源和规则绑定必须保留。
- 验收标准：离开后可恢复已完成任务；失败显示原因并重试；取消不会展示中间结果；可切换旧版本。

## 10. 视觉系统与端到端验收

- [ ] 修改目标：应用 UI&UX Token，完善响应式布局并完成主流程端到端验证。
- 允许修改范围：前端样式、组件视觉细节、端到端测试、本文件。
- 不允许破坏的逻辑：不引入自由画布、装饰性渐变或未授权素材；不改变任何已确认的数据和阶段边界。
- 验收标准：三页视觉统一；长文本、多图、加载、空数据、失败、取消状态均无重叠或溢出；完整主流程通过。
